import React from 'react';
import './Blogs.css'

const Blogs = () => {
    return (
        <div className='blogs-area'>
            <h1>How will you improve the performance of a React Application?</h1>
            <p>Optimizing reactions renders the material only progeny of the breed. The cpu helps to reduce the cost and avoid excessive rendering of unneeded braces. Replica rendering collects all the props to create a functional component. To prevent unwanted renders, you need to use portfolio components. Symbols rendering lazy loading.</p>

            <h1>What are the different ways to manage a state in a React application?</h1>
            <p>Data state control state sessions are used to manage the state, data state, control state, location state, useRef (), usecontext props, etc. are used.</p>

            <h1>How does prototypical inheritance work?</h1>
            <p>Prototype inheritance is a feature of JavaScript that involves adding methods and features to objects. It is such a method that one object can be distinguished and inherited from another object. Prototype is an behavior recycled in the style of an object-oriented pop programming. This model is known as prototype, prototype based and sunnybinas. This prototype object is its own prototype.</p>

            <h1>You have an array of products. Each product has a name, price, description, etc. How will you implement a search to find products by name?</h1>
            <p>Find out the components of an error. Need to create an empty variable. Is to pass through the material with a loop. A description is a statement that describes some or all of the relationships. All array components must be stored in the product.</p>

            <h1> What is a unit test? Why should write unit tests?</h1>
            <p>Unit testing is a software development process where the best test of an application is eligible. This test is conducted during the development of software developers and qa staff. A unit is tested with a unit test. It is the property of a function, a vowel, a method and programming language. Unit testing is a type of software testing.</p>
        </div>
    );
};

export default Blogs;